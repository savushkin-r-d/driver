; easy_drv driver configuration.
; Place this file next to the driver DLL (e.g. PAC_easy_drv_LZ.dll).

[log]
; show_window: 0 - hide the driver log window, 1 - show it.
; A missing file or key defaults to 1 (previous behaviour). See issue #12.
show_window=1
